# -*- coding: utf8 -*-
import impala

help(impala)

# conn = impala.dbapi.connect(host='10.0.0.221', port=21050)

# cursor = conn.cursor()
# cursor.execute('SELECT count(*) FROM iproduct.praiseAndBBS LIMIT 100')
# print cursor.description  # prints the result set's schema
# results = cursor.fetchall()